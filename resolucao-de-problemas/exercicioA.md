<!--
    1 - recebe a lista de números do usuário;
    2 - instanciamos 2 variáveis: highest_value e second_highest_value recebendo inicialmente valor 0
    3 - para cada número da lista:
            - se o número for maior que o valor atual em "highest_value":
                atribua à "second_highest_value" o valor atual de "highest_value"
                atribua à "highest_value" o número.
            - se o número for apenas maior que "second_highest_value":
                atribua somente o valor desta variável com o número.
    4 - após a iteração da lista completa, teremos o segundo maior valor da lista armazenado na variável "second_highest_value"
 -->
